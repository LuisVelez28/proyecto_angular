import { Reconocimiento } from './reconocimiento';

describe('Reconocimiento', () => {
  it('should create an instance', () => {
    const directive = new Reconocimiento();
    expect(directive).toBeTruthy();
  });
});
